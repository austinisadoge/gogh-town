# 素材來源

## 小鎮

梵谷小鎮大作戰沿用了專案原有的 **Van Gogh's Town** 程序生成場景。未經改動的原始副本放在 `reference/van-goghs-town.original.html`。

| 檔案 | SHA-256 |
| --- | --- |
| `reference/van-goghs-town.original.html`（79,047 位元組） | `fae8f5213a56e3b14fea3f5c43860f24896f539daf12874052560b2ed2cf12cd` |
| `src/map.js` | `298c334bd44138622a902cfedefb256a633c728e6210b494221c86412928783d` |

亂數種子固定為 18881890。調色盤定義、筆觸著色器、幾何輔助函式、地形、房間、地標和生成順序，在 `createTown()` 裡原封不動保留。測試會把整段保留區塊拿去跟原始檔比對。遊戲的操控、碰撞轉接、介面、戰鬥、角色和電腦對手則是另外加上去的。原始參考檔沿用它原本的 CDN 匯入；實際可玩的遊戲則是把相依套件放在本機提供。

> ⚠️ **這段保留區塊不要動。** 它包含視角清單（`viewpoints`）的英文名稱，那些名字在遊戲裡並不會顯示出來。中文化時刻意沒有翻譯它們，改動一個字元就會讓保真測試失敗。

場景是用幾何和顏料生成的，沒有下載任何畫作或貼圖。裡面的取景參考了《臥室》、《夜晚的露天咖啡座》、《隆河上的星夜》、《麥田群鴉》和《向日葵》。原始場景與它的遊戲改編版都依本專案的 MIT 授權散布。

## 角色

角色的頭部使用 MakeHuman 官方 hm08 人體網格，以及 commit `a8bc2d54ff0ac92e78ff71431b1023eda42bf482` 的 30 組變形資料，授權為 **CC0 1.0 Universal**。[鎖定版本的清單](vendor/makehuman/SOURCES.json) 記錄了上游網址與雜湊值，[素材授權](vendor/makehuman/LICENSE.ASSETS.md) 也一併附上。

原創的 Blender 產生器負責重塑頭型、加上貼合的頭髮與鬍鬚、生成皮膚材質，並把細微的局部陰影烘焙進頂點色。它會輸出高／低細節的 GLB（高細節那顆頭帶眨眼變形），並存下一份可編輯的 Blender 場景。身體、帽子和衣服則是執行時生成的幾何。遊戲、角色工作室和肖像卡用的都是同一批組好的模型。**沒有內嵌任何博物館肖像的像素或外部皮膚照片。**

歷史外貌考據與藝術詮釋分開記錄在 [角色設計筆記](docs/CHARACTER-DESIGN.md)。體型、服裝、戰術定位和移動個性都是遊戲詮釋；有文獻依據的史實則另外標註。隊伍、塗鴉標語和勝利舞全是虛構的。

## 其他美術與相依套件

武器預覽圖是遊戲內實際武器幾何的算圖。隊徽和塗鴉是受各畫家作品啟發的原創 SVG。特效、環境材質和音效都在本機生成。沒有內嵌任何第三方字型。

Three.js **0.170.0** 與所需的 addons 依 MIT 授權附上，聲明放在 `vendor/THREE-LICENSE.txt`。建置工具不散布 Blender 或 MakeHuman 的應用程式碼。授權界線詳見 [THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md)。

## 驗證

`npm test` 會檢查原始場景是否被保留，同時驗證移動、武器、電腦對手和介面狀態的邏輯。實際的算圖表現、輸入捕捉、視覺品質和效能還是要在瀏覽器裡親眼確認，自動測試不能代替。
